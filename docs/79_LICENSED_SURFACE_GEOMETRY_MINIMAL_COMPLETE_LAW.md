# 79 — Licensed Surface Geometry Minimal-Complete Law

> **Status:** Constitutional law document. Registered as `LGE-L0` in
> `docs/14_PR_CHAIN_ROADMAP.md`.
>
> Law-only: no runtime code, no carriers, no enums, no new global
> `FailureCode`, no adapter/audit mutation, and no semantic/dalālah/ifādah/
> ḥukm/truth/certainty/reality outputs.

---

## §1 Constitutional definition

This law establishes a surface-only geometric branch for licensed numeric
structure over Arabic-form surfaces. The branch is constitutional only if:

1. every transition is licensed and auditable,
2. every output remains formal/surface-only,
3. no output claims meaning or judgment,
4. minimum-complete conditions are satisfied before any transition verdict.

---

## §2 Scope and branch identity

```text
FAMILY               = LGE
STEP                 = LGE-L0
STEP_KIND            = LAW_ONLY
DOMAIN               = SURFACE_NUMERIC_GEOMETRY
OBJECTIVE            = Licensed geometric surface transitions only
TRANSITION_RULE      = MINIMUM_COMPLETE_REQUIRED
```

This step is an origin law and does not ship runtime carriers or gate engines.

---

## §3 Surface-only token family (licensed at later steps)

The branch licenses the following surface token families as future admitted
runtime steps only:

- حرف + حركة token
- مقطع token
- الاقتصاد الصوتي token
- أدوات token
- مبنيات token
- وزن جامد token
- وزن مشتق token
- مشتقات token
- اسم / فعل / حرف surface closure token

These are geometric/formal carriers only. They do not produce meaning.

---

## §4 Surface slot families (licensed at later steps)

Licensed geometric slot families (surface-only):

1. الجملة الاسمية
2. الجملة الفعلية
3. شبه جملة

All are structural numeric slots; none are semantic proposition slots.

---

## §5 Surface relation-engineering slots (licensed at later steps)

Licensed geometrical engineering slot families (surface-only):

1. النسب الإسنادية
2. النسب التضمينية
3. النسب التقييدية

These are formal relation shapes, not semantic entailment claims.

---

## §6 Inflection-mark surface

The branch licenses a surface-only numeric layer for:

- العلامات الإعرابية الأصلية
- العلامات الإعرابية الفرعية

Inflection marks here are geometric boundary/state signals only.

---

## §7 Style surface closure

The branch licenses final style-slot surface categories:

- الأخبار
- الإنشاء

This closure is formal-style only and does not authorize meaning or ḥukm.

---

## §8 Minimum-complete transition rule

No transition is licensed unless all minimum-complete conditions are explicit:

1. origin law is declared,
2. branch step is declared,
3. constitutional chain position is declared,
4. trace is present,
5. rank ceiling is declared,
6. residual visibility is enforced,
7. forbidden outputs are declared.

A missing condition is a constitutional refusal.

---

## §9 Reserved successor steps (not shipped by this law)

`LGE-L0` reserves and licenses only the sequence below:

1. `LGE-C1` — surface token carrier runtime
2. `LGE-C2` — sentence-slot runtime
3. `LGE-C3` — relation-engineering slot runtime
4. `LGE-C4` — inflection-mark surface runtime
5. `LGE-C5` — style-slot runtime

No bundling of these steps into `LGE-L0` is permitted.

---

## §10 Forbidden surface

This law MUST NOT:

- ship any `src/` runtime code,
- ship any LGE carrier/gate/verdict implementation,
- claim semantic interpretation, ifādah, mafhūm, ḥukm, truth, certainty, or reality,
- mutate adapter/audit boundaries,
- bypass trace/rank/residual discipline,
- collapse LGE-C1…LGE-C5 into one runtime step.

Such moves are `FORBIDDEN_LEAP`.

---

## §11 Constitutional tests

Acceptance tests must prove:

1. this law document exists and declares the branch identity,
2. minimum-complete rule is explicit,
3. reserved successor steps are declared,
4. forbidden surface remains enforced,
5. chain-state sync is present in `docs/14` and `CLAUDE.md`.

---

## §12 Trace

```text
docs/79_LICENSED_SURFACE_GEOMETRY_MINIMAL_COMPLETE_LAW.md
→ tests/test_lge_l0_minimal_complete_law.py
→ docs/14_PR_CHAIN_ROADMAP.md
→ CLAUDE.md
```
